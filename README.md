# 🚀 Terraform Docker Nginx Deployment

This project demonstrates how to use **Terraform** to deploy an **Nginx web server** inside a **Docker container** using **Infrastructure as Code (IaC)** principles.

It automates Docker image pulling, container creation, and port mapping through declarative Terraform configuration.

---

## 📌 Features
- Infrastructure provisioning using Terraform
- Docker-based Nginx web server deployment
- Configurable port mapping using variables
- Clean and reproducible setup
- Beginner-friendly and interview-ready project

---

## 🛠️ Tools & Technologies
- **Terraform**
- **Docker Desktop**
- **Nginx**
- **Terraform Docker Provider (kreuzwerker/docker)**
- **Windows OS (Docker via WSL2)**

---

## 📂 Project Structure
```text
terraform-docker-nginx/
├── main.tf
├── variables.tf
├── outputs.tf
├── .gitignore
└── README.md
